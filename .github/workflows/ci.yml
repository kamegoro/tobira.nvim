name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # stylua, selene and the Python unit tests are all lightweight, independent,
  # and need no Neovim — so they share one checkout and run as parallel steps
  # (GitHub Actions step-level parallelism) instead of three separate jobs.
  checks:
    name: Checks (lint & scripts)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - parallel:
          - name: Check formatting (stylua)
            uses: JohnnyMorganz/stylua-action@v5
            with:
              token: ${{ secrets.GITHUB_TOKEN }}
              version: latest
              args: --check lua/ plugin/

          - name: Lint (selene)
            uses: AlejandroSuero/selene-linter-action@v1.0.0
            with:
              token: ${{ secrets.GITHUB_TOKEN }}
              args: --display-style=quiet lua/ plugin/

          - name: Scripts (Python unit tests)
            run: python3 -m unittest discover -s .github/scripts -p "test_*.py" -v

  # Only nightly here. The stable run is covered by the `coverage` job below,
  # which runs the exact same suite on Neovim stable (COVERAGE=1 still executes
  # every test) — so a separate stable matrix entry just ran it twice.
  test:
    name: Test (Neovim nightly)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Neovim nightly
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: nightly

      - name: Run tests
        run: |
          nvim --headless --noplugin -u tests/minimal_init.lua \
            -c "PlenaryBustedDirectory tests/spec/ {minimal_init = 'tests/minimal_init.lua', sequential = true}"

  # Runs the full suite on Neovim stable with coverage — doubles as the stable
  # test run (a failing test fails this job too).
  coverage:
    name: Coverage (100%) · Neovim stable
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Neovim stable
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable

      - name: Install lua5.1 and luacov
        run: |
          sudo apt-get install -y lua5.1 luarocks
          luarocks --lua-version=5.1 install --local luacov

      - name: Run tests with coverage
        run: |
          COVERAGE=1 nvim --headless --noplugin -u tests/minimal_init.lua \
            -c "PlenaryBustedDirectory tests/spec/ {minimal_init = 'tests/minimal_init.lua', sequential = true}"
          LUA_PATH="$HOME/.luarocks/share/lua/5.1/?.lua;$HOME/.luarocks/share/lua/5.1/?/init.lua;;" \
            lua5.1 -e "require('luacov.reporter').report()"

      - name: Print coverage report
        if: always()
        run: cat luacov.report.out 2>/dev/null || echo "(no report generated)"

      - name: Enforce 100% coverage on core/ modules
        run: python3 .github/scripts/check_coverage.py luacov.report.out
